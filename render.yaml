services:
  - type: web
    name: nestbackend
    env: node
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm run prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
